# Unused — AI answer card hero exhibit

Built, reviewed, and rejected. Kept only so the work is recoverable; nothing imports it
and it is excluded from `astro check` via `tsconfig.json`.

An assistant answering "who can embroider 300 hoodies in DFW with a two week turnaround?"
as a hard-edged pixel card. Its final state is the markup — the client's shop already at
position one, the closing line already reading "Now it is." — so with JavaScript off or
`prefers-reduced-motion` set it renders correctly as a static image. The script only
rewinds to the earlier state and plays forward into what the HTML already says.

Delete this folder freely.
