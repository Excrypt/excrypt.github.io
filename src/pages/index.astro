---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/sections/Hero.astro';
import Capabilities from '../components/sections/Capabilities.astro';
import Shift from '../components/sections/Shift.astro';
import WhatWeDo from '../components/sections/WhatWeDo.astro';
// The three-step "Audit, build, then every month" section is archived, not
// deleted — see docs/archive/how-it-works/ to bring it back.
import Research from '../components/sections/Research.astro';
import WhoWeAre from '../components/sections/WhoWeAre.astro';
import Offer from '../components/sections/Offer.astro';
import Faq from '../components/sections/Faq.astro';
// Held back, not deleted: the closing band is written and styled, but the
// offer it makes is still being settled. Re-add both lines to bring it back.
// import CtaBand from '../components/sections/CtaBand.astro';
import GridBand from '../components/GridBand.astro';

import { site, absoluteUrl } from '../data/site';
import { homeFaq } from '../data/faq';
import {
  organizationSchema,
  webSiteSchema,
  localBusinessSchema,
  serviceSchema,
  personSchema,
  faqSchema,
} from '../lib/schema';

const schemas = [
  organizationSchema(),
  webSiteSchema(),
  localBusinessSchema(),
  serviceSchema(),
  ...site.founders.map(personSchema),
  faqSchema(homeFaq, absoluteUrl('/')),
];
---

<BaseLayout path="/" schemas={schemas}>
  <Hero />
  <div class="rails">
    <Capabilities />
  </div>
  <div class="rails">
    {/* From the third section down, a band separates each one from the next.
        Each carries a different arrangement so the rhythm reads as deliberate
        rather than as one ornament repeated six times. Blocks park themselves
        when off screen, so only the two or three bands actually in view are
        painting at any moment. */}
    <GridBand
      blocks={[
        { m: 1, spanM: 2, t: 2, spanT: 2, d: 2, spanD: 3, tone: 'navy' },
        { m: 4, t: 6, d: 9, tone: 'sky', edge: 'bottom' },
      ]}
    />

    <Shift />

    <GridBand
      blocks={[
        { m: 2, t: 3, d: 3, tone: 'lime', edge: 'bottom' },
        { m: 4, spanM: 2, t: 5, spanT: 2, d: 7, spanD: 3, tone: 'accent' },
      ]}
    />

    <WhatWeDo />

    <GridBand
      blocks={[
        { m: 2, spanM: 2, t: 2, spanT: 2, d: 3, spanD: 3, tone: 'navy' },
        { m: 5, t: 6, d: 9, tone: 'lime', edge: 'bottom' },
      ]}
    />

    <Research />

    <GridBand
      blocks={[
        { m: 2, t: 3, d: 4, tone: 'accent', edge: 'bottom' },
        { m: 4, spanM: 2, t: 6, spanT: 2, d: 8, spanD: 3, tone: 'sky' },
      ]}
    />

    <WhoWeAre />

    <GridBand
      blocks={[
        { m: 1, spanM: 2, t: 1, spanT: 2, d: 1, spanD: 3, tone: 'sky' },
        { m: 4, t: 6, d: 8, tone: 'navy', edge: 'bottom' },
      ]}
    />

    <Offer />

    <GridBand
      blocks={[
        { m: 2, t: 2, d: 2, tone: 'accent' },
        { m: 4, spanM: 2, t: 5, spanT: 3, d: 6, spanD: 4, tone: 'lime', edge: 'bottom' },
      ]}
    />

    <Faq />
  </div>

  <GridBand
    blocks={[
      { m: 1, spanM: 2, t: 1, spanT: 2, d: 1, spanD: 2, tone: 'lime', edge: 'bottom' },
      { m: 3, t: 4, d: 5, tone: 'navy' },
      { m: 5, t: 6, spanT: 3, d: 8, spanD: 4, tone: 'accent', edge: 'bottom' },
    ]}
  />

  {/* <CtaBand /> */}
</BaseLayout>
